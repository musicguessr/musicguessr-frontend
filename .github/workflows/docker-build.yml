name: Build and Push multi-arch Docker image

# Runs only after Frontend CI succeeds for a push to main — the Pi's Quadlets
# auto-update from :latest, so publishing in parallel with (instead of after)
# lint/tests/e2e used to ship images whose checks had failed.
on:
  workflow_run:
    workflows: [Frontend CI]
    types: [completed]
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push')
    runs-on: ubuntu-latest
    env:
      COMMIT_SHA: ${{ github.event.workflow_run.head_sha || github.sha }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.COMMIT_SHA }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a # v4.3.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Login to GitHub Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push multi-arch image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          # Only the commit tag here; :latest is moved in the next step.
          tags: |
            ghcr.io/${{ github.repository_owner }}/musicguessr-frontend:${{ env.COMMIT_SHA }}
          build-args: |
            GIT_COMMIT=${{ env.COMMIT_SHA }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Two merges in quick succession start two builds, and the older one can
      # finish last — it used to overwrite :latest with the older image (the
      # Pi auto-updates from :latest). :latest now only moves if this commit
      # is still the tip of the branch; otherwise the newer run owns it.
      - name: Tag as latest if still the branch tip
        env:
          BRANCH: ${{ github.event.workflow_run.head_branch || github.ref_name }}
          IMAGE: ghcr.io/${{ github.repository_owner }}/musicguessr-frontend
        run: |
          tip=$(git ls-remote origin "refs/heads/$BRANCH" | cut -f1)
          if [ "$tip" != "$COMMIT_SHA" ]; then
            echo "Skipping :latest — $BRANCH has moved on to $tip"
            exit 0
          fi
          docker buildx imagetools create -t "$IMAGE:latest" "$IMAGE:$COMMIT_SHA"
